create table o(
id number not null,
name varchar2(50) not null,
memo clob not null,
wdate timestamp DEFAULT current_timestamp,
primary key(id)
);

create SEQUENCE seq_memo start with 1
  INCREMENT BY 1 minvalue 1;